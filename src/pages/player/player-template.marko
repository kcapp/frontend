import Layout from "../layout.marko"

<${Layout}>
  <@body>
    <player player=input.player statistics=input.statistics previous_statistics=input.previous_statistics progression=input.progression checkouts=input.checkouts tournament_standings=input.tournament_standings/>
  </@body>
</>

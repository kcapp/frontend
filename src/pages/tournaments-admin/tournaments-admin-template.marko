import Layout from "../layout.marko"

<${Layout}>
  <@body>
    <tournaments-admin groups=input.groups players=input.players offices=input.offices 
      venues=input.venues types=input.types modes=input.modes
      presets=input.presets />
  </@body>
</>
